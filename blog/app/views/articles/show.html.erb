<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Comments</h2>
<!-- Rendering Partial Collection for not repeat the same code so render @article .comments and removing code -->
<%= render @article.comments %>

<h2>Add a comment:</h2>
<!-- Rendering Partial Collection for not repeat the same code so render @article .comments and removing code -->
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>